
export const ButtonIcon = (props)=>{
    const {Icon} = props;
    return(<div>
        <button className="border border-gray-500 rounded-full bg-white p-1 shadow-lg font-bold">{Icon}</button>
    </div> );
};

const Button = ()=>{
    return(<>

    </>);
};

export default Button;